package com.example.myapplication;

import java.lang.reflect.Array;

public class Subject {

    public static String subject[] = {
            "네트워크",
            "정보보호론",
            "시스템보안",
            "창의설계 프로젝트",
            "자바소프트웨어",
            "마이크로컴퓨터",
            "디지털포렌식"
    };
    /*public static String time(String str){
        int day[] = new int[Timetable.day];
        String t;
        for(short a = 0;a < day.length;a++)        day[a] = Timetable.max * a;
        if(str.compareTo("네트워크") == 0){
            time = new short[3];
        }
        for(int a = 0;a < Subject.subject.length;a++)
            if(str.compareTo(Subject.subject[a]) == 0)     break;
    }*/
}
